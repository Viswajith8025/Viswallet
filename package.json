{
  "name": "viswallet",
  "version": "1.0.0",
  "private": true,
  "description": "Viswallet — Next.js PWA personal finance tracker",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix web",
    "lint": "npm run lint --prefix web",
    "test": "npm run test --prefix web",
    "security:check": "npm run security:check --prefix web"
  }
}
